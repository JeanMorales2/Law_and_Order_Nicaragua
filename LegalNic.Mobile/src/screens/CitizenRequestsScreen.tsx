import { AppHomeScreen } from "./AppHomeScreen";

export function CitizenRequestsScreen() {
  return (
    <AppHomeScreen
      message="Esta seccion mostrara las solicitudes creadas por el ciudadano contra el backend real."
      subtitle="Ciudadano"
      title="Solicitudes"
    />
  );
}
